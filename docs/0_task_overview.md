# 要望の再構築

本プロジェクトはオンライン対戦型テトリスで、AWS上にコンテナ＋サーバーレス構成を採用し、運用コストを低減します。  
現在の実装では、ゲーム開始後にメイン画面 (`<TetrisGame>` コンポーネント) が表示されない不具合があります。  
さらに、`useEffect` を多用している箇所を可能な限り React Suspense を用いた遅延レンダリングに置き換え、初期ロード／データ取得のレイテンシを低減する必要があります。
